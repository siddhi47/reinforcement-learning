# reinforcement-learning
reinforcement learning series

install the requirements

```
pip install -r requirements.txt
```

run the tests
```
pytest . -s
```

`-s` flag is used to view the print statements in the code. Will be removed later

